#include <stdio.h>

/**
 * main - Entry point
 * Return: O always
 */
int main(void)
{
	int a, b;
	int add, sub, mul, div, mod;

	add = a + b;
	sub = a - b;
	mul = a * b;
	div = a / b;
	mod = a % b;

	return (0);
}
